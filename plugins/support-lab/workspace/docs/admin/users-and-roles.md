# Users and roles

Roles: `viewer`, `editor`, `tenant_admin`. Only `tenant_admin` can change tenant configuration and enable integrations. Role changes and configuration changes are written to `ops/config-audit.log` with the acting user.
